language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"

evn:
  - PYTHONPATH=$TRAVIS_BUILD_DIR/dragonchain

sudo: required

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script:
  - python blockchain/tests/*.py
